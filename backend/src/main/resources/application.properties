# Server
server.port=8080

# Database
spring.datasource.url=jdbc:mysql://localhost:3306/finsecure_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Root@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Increase max request size to accommodate base64-encoded PDFs (5 MB PDF → ~6.7 MB base64)
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
# Spring's Jackson / request body size limit
server.tomcat.max-http-form-post-size=20MB
server.tomcat.max-swallow-size=20MB

# JWT
app.jwt.secret=finsecure-super-secret-key-2024-production-minimum-256-bits-required
app.jwt.expiration=86400000

# OTP
app.otp.expiration-minutes=5

# Mail
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mavuikakionzigi@gmail.com
spring.mail.password=iwcj jqrb xort aydp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
app.mail.simulation=false

# Logging
logging.level.com.finsecure=INFO
logging.level.org.springframework.security=WARN